import type {Metadata} from "next";import {ProjectCard} from "@/components/project-card";import {RouteHero} from "@/components/route-hero";import {projects} from "@/lib/projects";
export const metadata:Metadata={title:"Projetos",description:"Projetos de software, dados, machine learning e IA aplicada de Victor Castro.",alternates:{canonical:"/projects"}};
export default function ProjectsPage(){return <><RouteHero className="projects-header"><div className="container"><span className="eyebrow">Projetos</span><h1 className="page-title">Projetos e estudos de caso.</h1><p className="lead">Uma seleção de projetos que desenvolvi ou em que estou trabalhando, com detalhes sobre o problema, a implementação e o estágio atual de cada um.</p></div></RouteHero><section className="container section"><div className="grid project-grid">{projects.map(p=><ProjectCard key={p.slug} project={p}/>)}</div></section></>}
